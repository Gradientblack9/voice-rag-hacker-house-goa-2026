# Runtime data

`scripts/ingest.py` creates `index.json` here from the official `ai4bharat/MSMARCO-XI` dataset. The generated index is intentionally ignored by Git: it can be rebuilt from the documented source and should not be committed.
